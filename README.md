# FOS v1.7.5 — Consistent Accounts & Bill Cycles

This build removes duplicate opening-balance controls, keeps the Accounts page as the single balance source, preserves the separate Sadaqah account, removes the Sadaqah-spending form, applies Sadaqah before Emergency allocation, and colour-codes bills by current/future cycle.

All existing localStorage data remains compatible.
